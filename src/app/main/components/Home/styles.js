import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  text: {
    color: 'red',
  },
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
